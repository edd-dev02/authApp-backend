# Backend en Nest

## Inicializar el contenedor con Mongo
```
docker compose up -d
```

## Iniciar el servicio del backend
```
nest start --watch
```

- Copíar el ```.env.template``` y renombrarlo a ```.env```